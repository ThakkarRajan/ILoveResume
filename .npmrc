# More reliable installs on CI (Vercel) when registry connections drop mid-download
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000

# Faster, less noisy CI installs
audit=false
fund=false
